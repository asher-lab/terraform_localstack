
# In case of additional requirements

rds -> needs outside monitoring

then we can define inside the rds a folder called 'peripherals'

OR

-!!


IF there is a need for other dependencies on the modules,
then we define the other componennts;

composition
modules
dependencies_modules